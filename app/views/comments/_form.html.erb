<%= render "layouts/errors", object: @comment %>
<%= bootstrap_form_for([@post, @comment]) do |f| %>
  <%= f.text_area :body, placeholder: 'Join the conversation.', hide_label: true  %>
  <%= f.submit 'Comment', data:{disable_with: 'Commenting....'}, class: 'btn btn-primary btn-block' %>
<% end %>
