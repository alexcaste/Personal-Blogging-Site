<%= render "layouts/errors", object: @comment %>
<%= bootstrap_form_for [@post, @comment], remote: true do |f| %>
  <%= f.text_area :body, placeholder: 'Join the conversation.', hide_label: true  %>
  <%= f.submit 'Submit', data:{disable_with: 'Commenting....'}, class: 'btn btn-primary btn-block' %>
<% end %>
